services:
  api:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: product-management-api-11

    env_file:
      - .env

    ports:
      - "8000:8000"

    restart: unless-stopped
